# Environment Variables

Copy this file to .env and fill in your actual values:

# Dobby API Configuration
VITE_DOBBY_API_URL=https://api.fireworks.ai/dobby/definition
VITE_DOBBY_API_KEY=REPLACE_WITH_YOUR_ACTUAL_API_KEY

## Getting Your API Key

1. Visit the Fireworks AI platform
2. Create an account or sign in
3. Navigate to the API section
4. Generate a new API key
5. Copy the key and replace REPLACE_WITH_YOUR_ACTUAL_API_KEY in your .env file

## Security Note

Never commit your .env file to version control. The .env file is already included in .gitignore.